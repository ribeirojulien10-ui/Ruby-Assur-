// PLACEHOLDER — remplacer par le vrai logo plus tard
export function Logo({ className = "" }: { className?: string }) {
  return (
    <span className={`font-display text-xl font-bold tracking-tight text-navy ${className}`}>
      Ruby<span className="text-gold"> Assur'</span>
    </span>
  );
}
